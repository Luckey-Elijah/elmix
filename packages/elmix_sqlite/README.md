# elmix_sqlite

SQLite storage adapter for Elmix.

This package will implement the Engine storage contracts for SQLite. Core v0 is SQLite-first, but the Engine must remain independent of SQLite APIs and SQL strings.

