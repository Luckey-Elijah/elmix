name: elmix_server
description: HTTP and admin API server layer for Elmix.
version: 0.1.0-dev
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  elmix_engine: 0.1.0-dev

