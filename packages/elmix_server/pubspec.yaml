name: elmix_server
description: HTTP and admin API server layer for Elmix.
version: 0.1.0-dev
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  crypto: ^3.0.6
  elmix_engine: 0.1.0-dev

dev_dependencies:
  test: ^1.31.1
