# elmix_engine

Core Elmix runtime and domain contracts.

This package owns Collection Schemas, Records, Access Rules, Action Hooks, execution context, storage contracts, and use-case orchestration. It must not depend on HTTP, SQLite, Jaspr, or CLI concerns.

